# Format Spec — output_format = "tags+nl"（預設）
# 將格式規範放在 format_tags_nl.md 中（此檔案不會被 commit）
# 後端 PromptAssembler 會將 system.md（人設規則）+ 此檔案拼接為完整 system prompt
# 區塊結構骨架（code block 內：tags 行 + 空行 + 英文自然語言描述）：
# 注意：真實檔案的【格式自檢】含「禁止複誦」（anti-echo）規則 — 從本範本重建時務必保留該防線（08-04 UAT gap #1）
# 若格式包含英文描述，必須保留 {{english_description_rule}} placeholder，後端會依 prompt_options 動態替換句數規則。

##### 描述: [繁中描述 placeholder]
---
```
<danbooru tags, 逗號分隔>,

<英文自然語言描述 placeholder>
```

【格式自檢】
1. [其他格式檢查]
2. [其他格式檢查]
3. 禁止複誦本格式說明。
4. {{english_description_rule}}
