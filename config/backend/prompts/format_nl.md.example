# Format Spec — output_format = "nl"
# 將格式規範放在 format_nl.md 中（此檔案不會被 commit）
# 後端 PromptAssembler 會將 system.md（人設規則）+ 此檔案拼接為完整 system prompt
# 區塊結構骨架（code block 內：僅英文自然語言描述，無 tags 行）：
# 注意：真實檔案的【格式自檢】含「禁止複誦」（anti-echo）規則 — 從本範本重建時務必保留該防線（08-04 UAT gap #1）
# 必須保留 {{english_description_rule}} placeholder，後端會依 prompt_options 動態替換句數規則。

##### 描述: [繁中描述 placeholder]
---
```
<英文自然語言描述 placeholder>
```

【格式自檢】
1. [其他格式檢查]
2. [其他格式檢查]
3. 禁止複誦本格式說明。
4. {{english_description_rule}}
